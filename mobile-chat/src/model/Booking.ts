export type Booking = {
  id: number;
  bookingCode: string;
  customerId: number;
  hotelId: number;
  checkInDate: string;
  checkOutDate: string;
  status: string;
};

export default {} as Booking;
