import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  conversationBox: {
    flex: 1,
    padding: 10,
    margin: 12,
    borderWidth: 1,
    borderRadius: 10,
    borderColor: "#dbd8d8",
    backgroundColor: "#FFF",
  },
});

export default styles;
